from datetime import datetime
from airflow import DAG
from airflow.operators.python import PythonOperator
from slack_alert import send_slack_alert  # Importing the alert function above

default_args = {
    'owner': 'zeliha',
    'depends_on_past': False,
    'email_on_failure': False,
    'on_failure_callback': send_slack_alert,  # <-- Automatically triggers on any task failure!
    'retries': 1,
}

with DAG(
    'data_quality_pipeline',
    default_args=default_args,
    description='Production-Grade Medallion Pipeline with Data Quality Gates',
    schedule_interval='@daily',
    start_date=datetime(2026, 1, 1),
    catchup=False,
) as dag:

    # Your existing tasks will go here...
    pass